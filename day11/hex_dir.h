#if !defined HEX_DIR_H
#define HEX_DIR_H

#include <stdint.h>
#include <stdlib.h>
extern int arr_max(int* arr, size_t size);
extern int distance(int* arr, size_t size);
extern void arr_sum(int *arr, char *tosum, size_t size);
#endif
